# 1. 代数证明题
一元线性回归：
$$\hat y_i=\hat\beta_0+\hat\beta_1 x_i$$

残差定义：
$$e_i = y_i-\hat y_i = y_i-\hat\beta_0-\hat\beta_1 x_i$$

最小二乘法最小化残差平方和：
$$RSS=\sum_{i=1}^n e_i^2=\sum_{i=1}^n(y_i-\hat\beta_0-\hat\beta_1 x_i)^2$$

对参数求偏导，令偏导数等于0：
$$\frac{\partial RSS}{\partial \hat\beta_0} = -2\sum_{i=1}^n\left(y_i-\hat\beta_0-\hat\beta_1 x_i\right)=0$$
$$\frac{\partial RSS}{\partial \hat\beta_1} = -2\sum_{i=1}^n x_i\left(y_i-\hat\beta_0-\hat\beta_1 x_i\right)=0$$

1. 由第一个方程：
$$\sum_{i=1}^n\left(y_i-\hat\beta_0-\hat\beta_1 x_i\right)=\sum_{i=1}^n e_i = 0$$
$\boldsymbol{\sum_{i=1}^n e_i=0}$，得证。

2. 由第二个方程：
$$\sum_{i=1}^n x_i\left(y_i-\hat\beta_0-\hat\beta_1 x_i\right)=\sum_{i=1}^n x_i e_i = 0$$
$\boldsymbol{\sum_{i=1}^n x_i e_i=0}$，得证。

---

# 2. 理解思考题
公式：
$$
R^2 = 1-\frac{RSS}{TSS},\quad R^2_{\text{adj}} = 1-\frac{RSS/(n-p-1)}{TSS/(n-1)}
$$

- $RSS$：残差平方和；$TSS$：总平方和；$n$：样本量；$p$：自变量个数；$n-p-1$ 为残差自由度。

1. **$R^2$ 上升或保持不变**
$TSS$仅由响应变量$y$决定，添加新变量不会改变$TSS$。
加入任意自变量（即使是随机无关特征），最小二乘算法会使用该变量拟合样本随机噪声，**$RSS$不会增大，只会减小或不变**。
由 $R^2=1-\frac{RSS}{TSS}$，$RSS$下降则$R^2$上升；变量完全无解释力时$RSS$不变，$R^2$不变。
> $R^2$不惩罚自变量个数，加变量不会使$R^2$降低。

2. **$R^2_{\text{adj}}$ 会下降**
$R^2_{\text{adj}}$引入自由度惩罚项。
新增无关变量，自变量数$p$增加，**残差自由度 $n-p-1$ 减小**。
无关变量仅能小幅降低$RSS$，但自由度减少带来的惩罚作用更强，使得 $\frac{RSS}{n-p-1}$ 变大，最终 $R^2_{\text{adj}}$ 下降。